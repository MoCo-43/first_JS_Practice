// function3_2.js

// 2. sumUpTo(5) => "1부터 5까지의 합은 15입니다."

function sumUpTo(number = 0) {
  let sum = 0;
  for (i=number; number > number-i; --i) {
    sum = number + number

  }
  
    //sum = number + (number-n)
}

sumUpTo(5);